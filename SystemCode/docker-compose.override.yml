# Auto-generated by start.bat — NVIDIA GPU acceleration for Ollama
services:
  ollama:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
